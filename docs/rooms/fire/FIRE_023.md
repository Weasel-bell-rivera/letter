# FIRE_023：灰色背景道路基础房间

## 状态与范围

- 灰盒中；用户2026-09-06明确要求创建类似FIRE_022的FIRE_023；沿用其60格道路与灰色背景；用户随后明确要求移除所有山体。
- Scene：`Assets/Scenes/Levels/Fire/Fire_023.unity`，手工Scene为权威，无房间运行脚本。
- 依据：GAME_DESIGN、MIRROR_MECHANIC、LEVEL_BUILDING、LEVEL_GEOMETRY_SYSTEM、CAMERA_SYSTEM、PLAYER_PREFAB、RESET_SYSTEM与FIRE_REGION现行规则。灰色底为本次明确批准的基础表现。
- MAP登记独立房间，未批准邻接连接；不推断FIRE_021→FIRE_023，不设置出口，不改世界进度。
- 定位：基础场景制作，无教学谜题、通关判定或失败压力。已添加一扇门与一块下沉永久开关；无敌人、Spawner、拾取物及岩浆。保留道路与灰色底，已移除远山及三座中景山。

## 网格、入口与通用对象

- Grid：1×1 unit；Terrain现为Cell X=-10..109、Y=-4..-1，120列×4行，共480格；顶面Y=0，形成一条120 units长连续黑色道路（由原60格向右延长）。
- 复用`Assets/Tiles/Fire/Fire021BlackTerrain.asset`纯黑Tile，已存在于`Assets/TilePalettes/Fire.prefab`，不重复创建或改共享Tile。
- Terrain使用Static Rigidbody2D、TilemapCollider2D与CompositeCollider2D，显式StaticSolid、安全、地面镜放置语义。
- DEFAULT入口(80,0.92)，朝右；标准0.8×1.8 Player碰撞盒底部净空0.02，无周边阻挡。
- 通用RoomPlayerSpawner2D经PlayerPrefabRegistry生成Player；RoomResetSystem绑定入口与本房CameraFollow2D；不序列化Player、不改镜像或移动规则。
- 两端开放，可手动重置回入口；没有新增坠落危险或自动坠落重置机制。固定敌人、出生点、Spawner需求均不适用。

```text
  P
############################################################  Y=0顶面
############################################################
############################################################
############################################################
X=-10                                                     X=49
```

## 背景、分层与相机

- 第1层：纯灰色#808080底，使用纯白方块Sprite及现有无光照剪影材质，Tint取灰色的线性值。背景中心(80,3)，60×18 units，覆盖X=50..110、Y=-6..12，与初始相机中心对齐；水平倍率1使其在相机行程中保持完整覆盖。
- Sorting Order=-100；ParallaxLayer2D水平倍率1、纵向不跟随，显式引用Main Camera。无Collider、Trigger、语义或其他玩法组件。
- 第6层：黑色Terrain、门、压力板和运行时角色；第2、3、4、5、7、8层无内容，省略。
- Camera灰色清屏作宽屏延伸；正交尺寸7，初始中心(80,3,-10)，仅水平跟随；显式偏移(0,0)、阻尼0（原0.15会产生停步后的屏幕回移，见文末排查），边界Rect(-10,-4,120,14)，Spawner和Reset均显式引用本房相机，序列化target为空。
- 16:9相机中心X范围约2.444..97.556，固定Y=3；入口按通用边界钳制。Player占屏约12.9%，不缩放角色。
- 必须可见当前Player、可站立边缘和镜子操作区域；不要求两端同时可见，不设计离屏双角色谜题。窄屏与完整行程构图待验证。

## 流程、死亡与恢复

1. 直接打开Scene，从DEFAULT生成Player，沿黑色道路移动，观察灰色底。
2. 通用跳跃与已解锁镜子能力可用，没有独立胜利条件或出口。
3. 手动重置及Player死亡沿用RESET_SYSTEM：回当前入口，镜子按解锁状态回手，MirrorClone清除；镜像单独死亡不重置Player。
4. 重新进入恢复静态场景，切场沿用通用清理规则，但本房没有切场出口。下沉开关若已完成，则按`FIRE_023:DOOR_GROUP:01`存档恢复按下与门开启；未完成恢复初始状态。

## 复用范围与资源

- 以FIRE_022当前磁盘Scene为新房间初始模板，更换房间根名称与Scene资源身份后移除山体；新Scene独立GUID，不复制图片、材质、Tile或.meta。
- 已按用户要求移除远山及三座中景山的Scene对象；不删除共享图片、材质或Fire_022对象。
- 灰底倍率1；所有背景均无碰撞和玩法语义。资产路径中Fire022仅代表共享素材来源，不是本房编号或玩法判断。
- 共享纯黑Tile已在Fire Palette中，无需追加。无正式出口、敌人或Spawner；永久状态仅包含下沉开关完成ID，不修改FIRE_022。

## 创建时验证记录（新增机关后的结果见文末）

| RV | 结果 | 证据与范围 |
|---|---|---|
| RV-01 | 静态通过，运行待验证 | DEFAULT(-8,0.92)，标准碰撞盒底部净空0.02，安全地面支撑 |
| RV-02、04、05、06、08 | 不适用 | 无出口、固定机关、门、敌人或Spawner |
| RV-03 | 静态通过 | 60格连续平面，上方没有玩法阻挡 |
| RV-07 | 静态通过 | 240格、原有Composite及StaticSolid配置；与模板地形序列化完全一致；装饰无Collider |
| RV-09 | 静态空间通过，运行待验证 | 开放平面净空，未改镜子规则；重复放置、回收与双角色移动未验证 |
| RV-10 | 静态配置通过，运行待验证 | 灰底倍率及相机边界引用完整；本房未拍摄运行画面，不能继承模板截图结论 |
| RV-11 | 静态引用通过，运行待验证 | Spawner、Reset、入口及相机引用保留，未执行死亡或手动重置 |
| RV-12 | 静态通过 | 新Scene GUID独立；道路与玩法配置继承模板，山体已移除；序列化anchor唯一；地图、索引、房间文档同步 |

- 创建时用户正在FIRE_022 Play Mode；只通过AssetDatabase复制单个Scene资源，再增量修改新Scene根名称。没有保存、切换或停止用户正在运行的场景。
- FIRE_023已落盘；未打开或运行新场景、未截图，未运行自动测试、完整编译或人工试玩。后续打开FIRE_023可直接运行；完整行程视差、出生/重置、双角色可读性仍待验证。
- 没有新增脚本、图片、材质、Tile或房间连接；已有工作区和FIRE_022内容保持不变。

## 移除山体

- 删除4个纯表现GameObject及其Transform、SpriteRenderer、ParallaxLayer2D，共16个序列化块，移除EnvironmentVisuals的4个子引用。
- RV-07、12静态检查通过：剩余240格道路不变，无悬空引用；灰底、相机、入口与玩法组件未改。RV-10运行画面未重验，其余项未受影响且本轮未重验；未运行自动测试或试玩。

## Prefab需求：门与开关（2026-09-09）

最初使用普通单压力板；用户随后明确要求替换为下沉永久开关，当前配置如下。

| 实例ID | Prefab | 世界位置 | 配置与安装 |
|---|---|---|---|
| FIRE_023_Door_A | `Assets/Prefabs/Gameplay/Doors/Door2D.prefab` | (8.5,1,0) | 初始关闭，唯一controlSource为FIRE_023_Plate_A，默认AND；旋转0、缩放1；标准1×2门体底部贴Y=0 |
| FIRE_023_Plate_A | `Assets/Prefabs/Gameplay/Switches/DescendingLatchSwitch2D.prefab` | (4.5,0.15,0) | DescendingLatch；永久ID=`FIRE_023:DOOR_GROUP:01`；旋转0、缩放1；Trigger为1.25×0.3，下沿Y=0，视觉安装基准Y=0 |

- 两实例位于Gameplay下并保留Prefab连接。Player或MirrorClone完整踩住持续1秒压到底才开门；中途全部离开取消进度并在0.2秒内回弹；到底后开关与门永久保持，防夹沿用Door2D。
- 当前为机关配置灰盒，门没有被声明为必经阻挡；不新增上方封墙，允许从上方绕行，不宣称构成必须使用镜像的通关谜题。
- 完成ID写入`latchedDoorGroupIds`；手动重置、Player死亡、切房和重启读档均保留已完成状态，只有新存档清除。未到底时重置恢复开关弹起、门关闭；镜像消失清理占用，若无其他有效踩踏者则取消未完成进度。
- 16:9默认相机在开关附近可同时观察门和开关；新增门实体范围X=8..9、Y=0..2，未与原有地面内部重叠。板完整踩踏中心区间约X=4.275..4.725（角色宽0.8）。
- 本轮只添加机关与文档；黑暗洞穴、右侧温泉绿光尚未在当前灰底Scene中实施。

### 本轮增量检查

- RV-01静态通过：新增对象远离DEFAULT入口；RV-03、04、05静态通过：地面支撑、标准门尺寸与网格对齐，门打开区域无Terrain；门非必经阻挡，不要求封闭上方路线。
- RV-07、08、12静态通过：原地形块未改变，两个Prefab GUID有效，门显式引用本房压力板；无新增脚本、共享Prefab或.meta修改。
- RV-09：未改变镜子规则，门周边实际放置与双角色控制待运行验证；未宣称解法通过。
- RV-10：16:9几何视野可同时覆盖两个机关；实际视觉和其他宽高比待验证。
- RV-11：复用门和压力板的统一重置实现；踩踏释放、防夹、镜像清理、死亡与重置运行结果待验证。
- RV-02、06不适用（无出口）；其余未受影响的入口、全路段和环境本轮未重验。
- 完成磁盘文本增量修改和静态检查；未操作Editor保存或Reload，Editor内存同步状态未确认。未进入Play Mode、未运行自动测试、完整编译或人工试玩。

## 门后背景层增量调整

- 按用户要求，复用FIRE_023_Door_A自带的PassageBackdrop图片，显式启用对象与Renderer；只覆盖本房实例。
- 背景子对象局部缩放(1.5,1.25,1)、局部位置(0,0.25,0)，扩大门后衬底；门体与Collider仍保持标准1×2。Sorting Order=-20，在灰底(-100)之前、门体(1)之后；不添加视差或碰撞。
- RV-07、08、12静态检查通过：仅新增背景表现覆盖，控制源、实体门、开关、地形及其他序列化块不变，Prefab连接保留。
- RV-10待验证：未进行Editor同步或截图，背景实际边缘与开关门状态下的可读性待运行观察。其他RV未受影响，本轮未重验；未运行测试或人工试玩。

## 门区局部残墙表现（2026-09-09）

- 在`EnvironmentVisuals/DoorSetting`下新增两个固定世界坐标纯表现对象：`DoorRuinWall`与`DoorRecessLight`。二者均不挂`ParallaxLayer2D`，也不含Collider、Trigger、Rigidbody2D、表面语义、镜面语义或玩法脚本；它们是与`FIRE_023_Door_A`对齐的机关装饰。
- `DoorRuinWall`中心(8.5,2.2,0)，缩放(1.18,1.35,1)，视觉范围约宽6、高4.4 units；轮廓左低右高、右侧更厚，门顶有一处破损缺口，两侧墙脚保留少量碎石，门口及压力板周围不铺碎石。使用`Assets/Art/Generated/Environment/Fire/OnDemand/fire023-door-setting/fire-mid-door-ruin-wall-v2.png`，由Labnana Nano Banana Pro生成并经项目色键脚本转换为真实RGBA；使用现有`Solid Silhouette Sprite`材质压平内部纹理，Tint为深暖中灰。Sorting Layer=`Default`，Order=-15。
- `DoorRecessLight`中心(8.5,1.335,0)，缩放(0.5,0.7,1)，约1.37×2.65 units，只比门洞略大；使用`fire-mid-door-recess-light-v2-clean.png`的弱灰散射中心表达凹入通道；该版本由同一Labnana色键原图提高去色键容差得到，避免边缘洋红溢色。Sorting Layer=`Default`，Order=-40。此前本房实例启用的Prefab `PassageBackdrop`覆盖改为禁用，避免开门后残留实心黑门板感；共享`Door2D.prefab`不变。
- 排序关系为灰底-100、门洞内衬-40、残墙-15、MirrorClone公共顺序-10、门体1、Player公共顺序10。排序完全由Sorting Layer与Order确定，不依赖Z坐标；门、残墙和内衬均固定世界坐标，相机水平移动时不会产生视差错位。
- 本轮保留灰色底，不增加远山、整房背景或完整八层素材。若运行画面仍显孤立，第二轮才评估第2层低对比远处洞壁；本轮不预先添加。

### 本轮增量检查

- RV-04、05、07、08、12静态通过：门、压力板、Terrain、Collider、控制源、门1×2尺寸与Transform未改；新增对象只有Transform与SpriteRenderer，且Prefab连接保持。门洞图片中央为空，墙脚碎石不跨越门口或压力板。
- RV-09未受影响且本轮未重验：未改变放镜表面、镜像生成空间、输入或生命周期。
- RV-10静态排序与构图范围通过，运行待验证：16:9下压力板(4.5,0.15)与门(8.5,1)仍处于同一可观察区；Editor当前未连接且macOS锁屏，关门、开门及左右相机移动后的实际像素对齐尚未截图确认。
- RV-11未受影响且本轮未重验：门与压力板继续复用通用重置和占用清理；未运行死亡、手动重置或防夹观察。
- RV-01、02、03、06及其他全路段内容未受本次纯表现增量影响，本轮未重验。未运行PlayMode自动测试、完整EditMode、完整编译或人工试玩。

## 替换为下沉永久开关

- 保留实例名`FIRE_023_Plate_A`、位置(4.5,0.15,0)、根缩放与旋转，Prefab改为`DescendingLatchSwitch2D`，显式模式2、永久ID=`FIRE_023:DOOR_GROUP:01`。门继续引用同一Scene组件23000202，原门及门区装饰不变。
- RV-04、07、08、12静态通过：仅替换开关Prefab来源、模式及存档ID；原地形、门、背景和所有非开关序列化块逐字保持，来源fileID存在，Scene锚点唯一，ID未与其他实例重复。
- RV-11静态配置通过、运行待验证：永久ID符合通用存档格式；下沉、中途回弹、镜像回收、死亡重置和重新进房后的锁存效果尚未运行观察。
- RV-10待验证：沿用相同位置和相机配置，新平顶剪影与实际按压画面尚未截图验证。RV-01、02、03、05、06、09未受此次替换影响，本轮未重验，不宣称全房或解法验收通过。
- 仅完成磁盘文本增量修改；未执行Editor保存/Reload，内存同步状态未确认。未运行Unity自动测试、完整编译或人工试玩。

## 修复实体石台支撑

- 新开关原实现只有Trigger，无法站上去；现已在共享Prefab配置Kinematic Rigidbody2D、独立非Trigger顶板Collider和安全DynamicSurface，并通过公共表面速度接口承载Player与MirrorClone。
- `FIRE_023_Plate_A`仍位于(4.5,0.15)，宽2.5，顶面初始Y=0.8、到底Y=0.2；横向范围X=3.25..5.75，完整踩踏中心区间X=3.65..5.35。下沉距离0.6，下降/回升各1秒，整体平移且不缩放。顶板最低底部允许沉入原地面，台面始终高于地面；不修改Terrain。
- 中途离开取消进度并回升，到底永久开门；保存ID不变。读取已完成存档时石台直接恢复最低位置。
- RV-03、04、07、08、12静态检查：石台远离入口和门体，来源Prefab保留，实体顶板及表面语义已序列化；原地形和Scene未改。RV-09、10、11运行待验证：实际跳上顶面、双角色承载、回升、镜像回收与读档后画面尚未在Editor验证。其余项本轮未重验；未运行自动测试或人工试玩。

## 石柱剪影样例落地（2026-09-09）

- 用户确认采用无纹理剪影，并要求加入原样稿和一根新绘远景石柱。此次仅添加两个独立Sprite模块，不扩展更多石柱或整房洞壁。
- `EnvironmentVisuals/04 Midground Pillar Sample`：原确认样稿PillarMid.png，中心(-3,3.3,0)，缩放1，PPU=192，画布世界范围5.333×8；Order=-50，水平视差0.65，纵向不跟随，颜色为深灰。
- `EnvironmentVisuals/03 Distant Pillar Sample`：新绘PillarFar.png，中心(1.5,3,0)，缩放0.85，画布范围4.533×6.8；Order=-80，水平视差0.85，纵向不跟随，以更浅灰色降低对灰底的对比。
- 两根底部延入地面剪影之后，仅为装饰，不含Collider、Trigger或表面语义。相机显式引用Main Camera；不修改门区、永久石台开关或Terrain。
- 资源位于`Assets/Art/Generated/Environment/Fire/Fire023Pillars/`。生成源图片没有真实Alpha（中景为烘焙棋盘格，远景为白底），当前通过专用FlatMatteSilhouette材质按亮度移除底色，输出统一颜色；不能把原PNG声明为透明图片。新Shader从已有SilhouetteMatteSprite实现派生，去掉内部明暗，仅用于表现；没有改共享Shader或玩法脚本。
- RV-07、08、12：Editor回读确认两对象没有Collider，Shader无编译错误、引用有效，保存后Scene非dirty；既有门、开关及地形未执行修改。
- RV-10：主相机编辑态截图`Captures/Fire023Pillars/pillar-editor-preview.png`确认两根轮廓可见、底色已去除，当前构图未覆盖门及开关。尚未进行Play Mode全行程观察，其他镜头位置、角色重叠时对比和视差动态仍待验证；不宣称全房视觉验收通过。
- RV-01～06、09、11其他玩法范围未受本轮纯表现修改影响，未重验。未执行PlayMode自动测试、完整测试或人工试玩。
- Unity W1@49ee0df8，6000.5.8f1，目标Scene已在Editor保存。保留本轮开始前的门区残墙、门洞内衬与下沉永久开关等现有修改。

## 左侧远景朦胧微光样例

- 用户要求实现一处远景朦胧微光。新增`EnvironmentVisuals/01 Left Distant Haze Glow`，中心(-2,3.7,0)，覆盖19×12 units，Order=-90，位于灰底(-100)之上、远景石柱(-80)之后。
- 复用现有`W1/Environment/RadialCaveGlow2D` Shader和白色Sprite；独立材质`Fire023Pillars/Fire023DistantHazeGlow.mat`。颜色(0.39,0.43,0.40,1)、Intensity=0.5、Falloff=2.3；径向透明衰减产生宽而柔和的灰绿雾光，没有硬边，不照亮玩法物体或增加危险语义。
- 第1层水平跟随倍率1、纵向不跟随，表达最远处空气光；本样例不是右端温泉的固定光源，不改变右端温泉方向设定。尚未添加洞壁图片或动态水汽。
- Unity中增量创建并保存，操作前Scene非dirty、非Play Mode、无Prefab Stage；仅新增一个无碰撞装饰对象及材质，未改共享Shader、门、开关、石柱或Terrain。
- RV-07、08、12静态通过：仅表现组件，排序与相机引用明确；复用Shader无编译错误。RV-10编辑态主相机截图通过：`Captures/Fire023Pillars/distant-glow-preview.png`，可见柔光与石柱前后关系且无矩形边界；动态视差、全行程和角色实际可读性仍待运行验证。其余RV未受影响，本次未重验；未运行Play Mode、自动测试或人工试玩。

## 扩充石柱与门后大柱

- 按用户要求保留微光，复用已确认两张无纹理剪影，新增7根石柱（连同原2根共9根），通过等比缩放、水平翻转和间距变化排列。
- 远景新增：(-7,3.85)缩放1.1、(18,3.325)缩放0.95、(34,4.2)缩放1.2，Order=-80、水平视差0.85。
- 中景新增：(14,5.25)缩放1.5、(29,4.2)缩放1.2、(44,5.775)缩放1.65，Order=-50、水平视差0.65；大柱延伸出画面顶部。以上均不纵向跟随。
- 门后`Door Back Pillar`中心(8.5,5.25)，等比缩放1.5，Order=-45，位于门洞内衬(-40)、残墙(-15)与门体之后；固定世界位置，不使用视差，避免与门错位。没有删除原门区装饰。
- 两张原图片、材质、微光与玩法配置保持；新增对象仅Transform、SpriteRenderer及适用的ParallaxLayer2D，不含Collider或玩法语义。
- RV-07、08、12：Editor增量创建并保存，引用现有资源，不修改Terrain、门、开关或共享Prefab。RV-10：编辑态初始主相机截图`Captures/Fire023Pillars/expanded-pillars.png`确认远近层次、大柱与门后柱可见；全行程视差、角色对比和其他宽高比待运行验证。其余RV未受影响且未重验，未运行Play Mode、自动测试或人工试玩。

## 前景钟乳石与石笋样例

- 按用户要求新绘一张上下独立剪影素材`Fire023Pillars/CaveForegroundSheet.png`，通过Unity Sprite Multiple切为Ceiling与Floor，不修改原图；复用FlatMatteSilhouette去白底，内部保持纯色。
- `08 Foreground Stalactites`中心(-2,7.7)，等比缩放2.6；顶部岩檐带三根钟乳石，从画面上缘垂下，避开道路与跳跃空间。
- `08 Foreground Stalagmites`中心(-3,-1.4)，等比缩放3；下缘延出屏幕，尖端保留在道路顶面以下，避免遮住角色脚下和石台。
- 两者Order=30、水平视差0.2、纵向不跟随，显式Main Camera引用；无Collider或玩法语义，不改变门、永久开关、地形、微光及石柱。
- RV-07、08、12静态配置通过：仅两个表现对象、同一新图片及Unity生成meta，复用现有材质；目标Editor非Play Mode、无未保存编辑及Prefab Stage，增量保存完成。
- RV-10：编辑态截图`Captures/Fire023Pillars/foreground-final.png`确认上下框景与道路、门和开关的可见性；全行程视差与角色运行遮挡仍待验证。其余RV未受影响，本次未重验；未运行Play Mode、自动测试或人工试玩。

## 道路长度加倍

- 用户要求地面变为原来的2倍；原60格长×4格厚，共240 Tile，现向右增加60列，成为120×4，共480 Tile，范围X=-10..109、Y=-4..-1。历史示意图与创建记录仍表示原60格版本，以本节及当前网格定义为准。
- 相机roomBounds宽度同步为120；正交尺寸7、固定Y=3不变。入口、机关、所有装饰原位保留；新增右半段暂为无机关和新增装饰的道路。
- RV-03、07、12静态通过：480唯一格坐标完整覆盖目标矩形，四项Tile引用计数480，复用原Tile、碰撞与表面语义；RV-10相机宽度配置同步，完整行程表现待验证。其余RV未受影响，本轮未重验。
- 修改前用户已进入Play Mode，因此未中断试玩，改为磁盘增量修改。未执行Reload或保存Editor内存版本；退出Play Mode后需安全同步磁盘场景。没有新的运行截图，不宣称试玩通过；未运行自动测试。

### 石台图片与碰撞对齐修复

- 改为明确的居中Sprite Pivot与Visual局部Y=0.25，消除自定义Pivot导入结果和旧视觉偏移叠加造成的悬空碰撞。基于2528×1696原图的黑色轮廓边界(295,538)..(2233,1159)、1550 PPU和2倍缩放计算：可见台面局部Y=0.65，与实体Collider顶面一致，宽约2.5006，与Collider宽2.5一致。
- RV-04、12像素坐标与序列化静态检查通过；Trigger、实体Collider、台面行程、永久ID与门引用不变。圆角两端只存在图片小圆角与矩形Collider的局部差异。Editor导入/Prefab Stage同步与运行截图未验证，不覆盖用户未保存编辑。

## 修复加长道路的碰撞缓存

- 用户报告右半道路掉落。Editor回读确认Tile覆盖X=-10..109，但CompositeCollider2D保存的轮廓仍截止X=50；前次仅文本增加Tile数据遗漏了碰撞几何同步。此前道路加倍记录中的RV-07静态通过不足以证明碰撞正确，以此次修复验证为准。
- 在非Play Mode、Scene非dirty且无Prefab Stage时，使用Unity Tilemap API重新提交现有480格数据，处理Tilemap碰撞变更并生成Composite几何，保存目标Scene。地形形状、表面语义、入口与机关不变。
- RV-07、12：合并碰撞边界现为X=-10..110、Y=-4..0，单一路径；磁盘缓存右边界110已写入。Editor物理查询逐列检查120个格中心，从Y=1向下均命中Terrain合并碰撞体，缺失0列。
- 未进行人工试玩、PlayMode自动测试或完整测试；以上为Editor物理查询证据，不声称主角完整行程试玩通过。其他RV未受影响，本轮未重验。

## 排查并消除停步后的屏幕回移

- 用户报告中段向右移动时仿佛被推回。运行时排查从门后X=10到106，861次物理步采样仅有Terrain向上的支撑接触，没有侧向障碍或位置倒退；记录`Captures/Fire023Motion/constant-input.csv`。不据此排除其他尚未复现的输入或镜像状态。
- 明确复现的现象：在X=60附近，短按右移0.4秒、松开0.6秒，连续四轮。原相机smoothTime=0.15时，角色物理位置不倒退，但屏幕横向位置每轮后移约0.737～0.749 units，来自停止后相机仍追赶目标。入口相机尚未接管跟随时不出现同样表现。
- 仅将本房CameraFollow2D阻尼从0.15改为0，保留通用跟随、入口接管、视野边界、镜头比例及Player Rigidbody插值；不修改公共脚本、基础移动参数、碰撞或镜像规则。房间可配置阻尼的依据见CAMERA_SYSTEM“相机配置与绑定职责”。
- 相同四轮运行观察中，修复后屏幕回移均为0，物理位置倒退0次；前后记录分别为`tap-input.csv`与`tap-fixed.csv`。该证据确认修复此短按/松键回跳，不声明所有用户输入情况下的阻挡问题均已排除。
- RV-10运行观察通过上述特定触发场景；RV-07碰撞观察覆盖门后直路，其他机关交互及镜像状态未重验。RV-12场景已在退出观察后保存，Editor非dirty。运行观察由agent启动并已退出，没有保存运行时角色位置；未运行Unity Test Framework自动测试或完整人工试玩。

## 右侧水洞：远处洞壁首层（2026-09-09）

- 用户明确要求先用Labnana制作远处美术并加入此前指定的右侧X=60～100。本轮只落地远处洞壁；水面、远岸、近处拱顶与两侧框景尚未制作，不声明完整水洞已经完成。
- Labnana Nano Banana Pro（`gemini-3-pro-image`）生成单张3168×1344远处火山洞壁，沿用本房已批准的中性灰色表现。主体为大块非对称岩层与宽凹陷，低对比、无密集裂纹和纹理，不含路线、地面、水池或机关。源图与完整提示词保存在`Captures/Fire023RightCave/`。
- 正式RGBA资源：`Assets/Art/Generated/Environment/Fire/OnDemand/fire023-right-cave/fire-far-cave-wall.png`。使用现有色键脚本确定性转透明，tolerance=100、edge-tolerance=200；Alpha边界(72,62)..(3096,1282)，透明像素约28.30%，四周有透明留白。Unity生成meta；Single Sprite、100 PPU、4096上限、无压缩、无MipMap、Clamp和Bilinear。
- 新增`EnvironmentVisuals/03 Right Cave Far Wall`，世界中心(80,5.2,0)，等比缩放约1.325758，画布42×17.818 units，实际轮廓X约59.955～100.045；底部延入黑色地面后方，顶部超过当前镜头。Default排序Order=-85，位于灰底(-100)之前、原远景柱(-80)、角色与Terrain之后；Tint=(0.55,0.55,0.55,0.8)。独立材质复用URP Sprite-Unlit-Default，不修改共享Shader。
- 历史首版（已由下节视差修正取代）：按用户指定的局部水洞位置固定世界坐标，不挂ParallaxLayer2D；这是本房局部布景落位，不宣称实现默认0.85远景视差，也不修改全局倍率。原因：若直接给X=80的对象套用0.85，相机从X=0移动至80时对象会漂至X=148，离开用户指定位置。后续水面与岸壁需以同一局部空间为基准。
- 对象只有Transform和SpriteRenderer，无Collider、Trigger、Rigidbody2D、表面语义、镜子语义或状态脚本；道路、门、永久石台、相机、入口与原有环境对象保留。

### 增量验证

- RV-07、08、12静态通过：Scene只新增3个序列化块及EnvironmentVisuals的一个子引用，所有其他原有序列化块保持一致；无删除和玩法配置变动。新Sprite与材质引用经Editor回读有效。目标Unity为W1@49ee0df8 / 6000.5.8f1，非Play Mode、无Prefab Stage、目标无未保存编辑时增量保存。
- RV-10编辑态检查：临时相机按主相机正交尺寸7、16:9、Y=3，在X=60、80、97.55观察；对应证据为`Captures/Fire023RightCave/editor-x60.png`、`editor-x80.png`、`editor-x98.png`。检查透明边缘、灰底衔接、前后排序和地面顶面。临时相机已销毁，未改主相机位置。尚未包含运行时角色与原有视差层移动，不能作为完整运行构图通过证据。
- RV-01～06、09、11不受本轮纯表现增量影响且未重验；无出口相关项维持原不适用状态。本轮未进入Play Mode，未运行PlayMode自动测试、完整测试、完整编译或人工试玩；角色对比、原有视差层叠加与其他宽高比仍待运行验证。
- 保留任务开始前全部未提交内容；没有修改地图、其他房间、共享Prefab或公共运行脚本。
- 最终补充：Console读取到0条Error；最终落盘差异再次确认只有3个新增块与一个父级子引用。截图后Editor出现dirty标记，未擅自再次保存或Reload，保留当前内存编辑；磁盘已保存本轮洞壁(80,5.2,0)的最终配置。

## 统一远景视差与右侧出生点（2026-09-09）

- 用户要求洞壁与其他远景速度相同，并将人物出生点移至右侧。本节取代上一节固定世界洞壁方案及旧左侧出生配置。
- `03 Right Cave Far Wall`新增通用ParallaxLayer2D，水平cameraFollowFactor=0.85，followHorizontal=true、followVertical=false，显式引用Main Camera，与原远景石柱一致；初始位置仍为(80,5.2,0)，Sprite、材质、排序与尺寸不变。
- DEFAULT入口由(-8,0.92)改为(80,0.92)，朝右不变；主相机初始中心由(0,3,-10)改为(80,3,-10)。灰色底中心同步至(80,3,0)，保持原60×18尺寸与倍率1，覆盖新初始镜头。相机正交尺寸7、阻尼0、120格边界、公共逻辑与其余装饰原位不变。
- RV-01、03、07：Editor完整0.8×1.8出生盒查询无实体重叠；脚底0.02净空，向下射线命中Terrain顶面Y≈0。Play Mode实际生成Player在(80,0.91)，正常地面支撑。地形与Collider未修改。
- RV-10：Play Mode初始相机(80,3,-10)、洞壁(80,5.2,0)。暂停后受控移动相机+4 units并调用现有视差更新，洞壁位移3.40000153、原远景柱位移3.39999962，与0.85倍率一致（浮点差异）。这是运行观察，不是Unity Test Framework自动测试或完整人工试玩。
- RV-11：同次暂停观察调用通用ResetRoom，Player返回(80,0.92)，相机返回(80,3,-10)，更新后洞壁回到(80,5.2,0)；未验证死亡、镜像生命周期或切房。
- RV-12：通过Editor增量保存；观察结束已退出Play Mode，不保存运行时状态。只变更本房Scene和文档，没有新增脚本、图片、共享Prefab或地图修改。RV-02、06无出口，不适用；RV-04、05、08、09及其他未影响范围未重验。未运行完整测试、完整编译或自动测试；全行程视差覆盖、其他宽高比与双角色可读性仍有待验证。

## 出生位置无关的视差基准修复（2026-09-09）

- 修复从右侧直接出生时原有柱子留在左侧、与从左侧走来构图不一致的问题。13个ParallaxLayer2D实例启用固定世界空间参考Pose；运行时不再以出生时相机位置重新定义构图。
- 原有柱子、前景及左侧雾光以相机(0,3,-10)为制作参考；灰色底和右侧洞壁以(80,3,-10)为参考，各图层参考位置取本次修改前的场景世界坐标。相机X=80时，远景中央柱X=86、中景右大柱X=96、洞壁X=80。倍率、Sprite、排序、地形及出生点不变。
- 固定参考模式在Awake首次应用，在LateUpdate按当前相机位置计算；CaptureReferencePose及相机重新获取也复用固定值，避免把运行中位移累积为新基准。其他房间未迁移，继续原有兼容行为。
- RV-07、08、12：本轮仅增量修改视差组件代码与13份参考Pose配置；保留GUID、对象层级与玩法组件。RV-10、11运行验证结果见下文；其余项目未受影响，本轮未重验，不代表全房验收通过。
- 验证：Unity 6000.5.8f1、W1@49ee0df8，重新加载已落盘Scene后13个实例固定参考字段回读正确，参考位置与制作时世界坐标一致。脚本定向导入后编译完成，Console Error为0。
- RV-10：Play Mode右侧初始化截图`Captures/Fire023ParallaxReference/right-spawn-fixed.png`确认原有柱子与洞壁同时出现。暂停下将相机依次设为X=0、20、60、80并调用视差更新，13层与右侧初始位置最大偏差0；重新调用CaptureReferencePose后偏差0。属于受控运行观察，不是自动测试或实际输入全行程试玩。
- RV-11：暂停下将相机移到X=96，再调用通用ResetRoom及视差更新，相机回到X=80，13层与初始位置最大偏差0。未验证死亡、镜像生命周期或场景切换。截图中近黑柱体与角色附近的明暗分离仍有限，不能据此宣称全房角色可读性验收通过。
- Scene通过文本增量落盘并安全Reload，未从旧Editor状态覆盖保存，完成后退出Play Mode。未运行自动测试、完整编译、全量导入或人工试玩；现有无关工作区改动保留。

## 温泉五张独立素材与场景配置（2026-09-09）

- 用户要求先画出水面、远岸、洞顶、左岩壁、右岩壁，并配置到已有右侧温泉。本轮使用Labnana Nano Banana Pro（gemini-3-pro-image）分别生成；只将五张最终RGBA图导入，不烘焙整房或玩法路线。水面首稿误含厚侧边，已针对该问题重画一次，采用无厚边水面版本；原稿仅保留在Captures，不进入Scene。
- 素材目录：`Assets/Art/Generated/Environment/Fire/OnDemand/fire023-spring-five/`。完整提示词、原始色键图、Alpha与去溢色记录：`Captures/Fire023SpringFive/`。宽图3168×1344，左右壁1696×2528；现有色键转换脚本采用tolerance=100、edge-tolerance=200，额外确定性清理可见洋红溢色（脚本与数量记录同目录），保留原始生成图。
- 五图均为真实Alpha、完整主体留白；透明比例约45.84%（顶）、72.63%（岸）、50.72%（左）、46.28%（右）、51.11%（水）。保留大块低对比岩层与少量水面横纹，无人物、镜子、玩法地形、危险或机关。
- Unity Single Sprite、100 PPU、Bilinear、Clamp、无MipMap，默认4096上限与无压缩；meta由Unity生成。五个对象复用上一轮URP Sprite-Unlit-Default材质，不修改共享Shader。以下位置为参考相机(80,3,-10)下的位置，各对象等比缩放。

| 对象（EnvironmentVisuals下） | 图片文件名 | 位置 | 等比缩放 | Order | 水平倍率 |
|---|---|---|---:|---:|---:|
| 03 Spring Water | fire-spring-water.png | (80,-2,0) | 0.946970 | -70 | 0.85 |
| 03 Spring Far Bank | fire-spring-far-bank.png | (80,0.6,0) | 1.010101 | -75 | 0.85 |
| 04 Spring Cave Roof | fire-spring-cave-roof.png | (80,11,0) | 1.199495 | -35 | 0.65 |
| 04 Spring Left Wall | fire-spring-left-wall.png | (65,6.3,0) | 0.632911 | -30 | 0.65 |
| 04 Spring Right Wall | fire-spring-right-wall.png | (95,6.3,0) | 0.632911 | -30 | 0.65 |

- Tint依次为等RGB 0.9、0.7、0.7、0.8、0.8，Alpha均1；现有远处洞壁不改。水面在Y=0地面后上方露出约2.1 units，下缘被原黑色Terrain遮住；远岸下半部被水面遮住，表现湖后岸，而非新增可站立平台。
- 所有新图层启用项目现有useFixedReferencePose，referenceCameraPosition=(80,3,-10)，referenceLayerPosition为表中坐标；只水平视差，显式引用Main Camera。水和岸保持同倍率同横向参考，不会随相机横移分离；洞顶和两壁是独立中景。工作期间发现已有13层的固定参考基准更新，本轮保留该更新，不修改公共脚本或恢复旧初始化方式。
- 为避免出生点Player与原中景黑柱混在一起，仅将`04 Mid Pillar CenterRight`的本房Renderer颜色由(0.035,0.035,0.035,1)调为(0.24,0.24,0.24,1)；保留其位置、图片、材质、排序和0.65视差。其余既有环境对象保持。本轮没有水汽、动态波纹、交互水体或涉水机制。

### 增量验收与运行证据

- RV-07、08、12：五个对象各仅Transform、SpriteRenderer、ParallaxLayer2D，共20个新增序列化块，EnvironmentVisuals新增5个子引用；无Collider、Trigger、Rigidbody2D或玩法语义。既有Terrain、门、永久石台、入口、相机参数和玩法组件不变。新Sprite、材质与相机引用回读有效。场景增量保存完成，无新公共运行脚本、Prefab或地图修改。
- RV-01：Play Mode从(80,0.92)的DEFAULT实际生成，落地后中心约(80,0.91)，未新增出生阻挡；未重验其他入口规则。RV-10：暂停运行下在X=60、80、97.55，Y=3、正交尺寸7，受控传送角色并更新视差取图，见`Captures/Fire023SpringFive/final-x60.png`、`final-x80.png`、`final-x98.png`。人物与地面轮廓可见，水岸无横向错位；这是指定位置的运行截图，不是完整人工试玩或自动测试。
- 同次运行观察：相机X=60/80/97.55时，水与岸的X均分别为63/80/94.9175；两者差值为0。RV-11：调用通用ResetRoom后Player回(80,0.92)、相机回(80,3,-10)，图层按固定参考恢复。未验证死亡、镜像操作、存档或场景切换。
- RV-02、06无出口，不适用；RV-03～05、09及其他未影响范围本轮未重验。运行仍可能在未采样位置与原有柱子重叠，其他宽高比、全程移动及双角色可读性待验证。不声明全房视觉或解法验收通过。
- 起初发现用户Play Mode，未中断；用户退出且目标Scene非dirty、无Prefab Stage后才在Editor增量写入。后续由agent启动的观察已退出，不保存运行时坐标。未运行PlayMode自动测试、完整测试、完整编译或全量导入；已有工作区改动保留。
