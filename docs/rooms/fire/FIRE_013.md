# FIRE_013：双门抉择

> 2026-08-31 显示调整：按用户要求关闭本房 `Decoration` 的 `Tilemap Renderer`，保留对象、Tile 与坐标；本文中的该层提示位置仅作设计参考，当前不显示、不再要求提示标记可见。碰撞、镜子放置规则与解法不变。

- 状态：灰盒中；用户已批准实现当前地图中可落实的后续房间。
- Scene：`Assets/Scenes/Levels/Fire/Fire_013.unity`。
- 连接：`FIRE_009`、`FIRE_014`、`FIRE_015`，不改变`docs/maps/MAP.md`。
- 入口与出口：保留唯一`DEFAULT`安全入口；FIRE_013至FIRE_017之间的已实现双向连接使用`FROM_<来源房间ID>`入口。
- 定位：分支枢纽与双门练习；固定单屏，Grid `1×1`，边界`X[-15,15] Y[-7,7]`，正交尺寸`7`。

## 布局与解法

```text
FIRE_014 ◀ E ─ D-A ─── [B] ─ M/P ─ [A] ─── D-B ─ E ▶ FIRE_015
```

Player与MirrorClone分居镜面两侧：右侧`Plate-A`持续开启左门，左侧`Plate-B`持续开启右门。玩家根据目标分支选择站位与镜子放置时机；中央短阶通向返回`FIRE_009`的上层出口。

## Prefab与重置

- `Plate-A (5.25,-1.85)`控制`Door-A (-5.5,-1)`；`Plate-B (-4.35,-1.85)`控制`Door-B (6.5,-1)`；两板均在两门之间。两门初始关闭，均由各自单板持续占用控制，不使用永久锁存。板位置不等于完整解法已验证。
- 两门、两板和三个`RoomExit2D`均使用现有通用Prefab；无敌人、Spawner、岩浆或房间脚本。
- Player死亡、手动重置和重新进入时恢复两门关闭、两板弹起；MirrorClone消失立即释放占用。
- 风险：未人工试玩，双分支的到达容错待验证。

## 2026-08-31 房间几何修复（已写入Scene）

本节记录视觉优化之后的玩法落位修复；下方视觉优化记录中的旧坐标、103格、出生失败和画外出口描述是历史证据，不再代表当前磁盘布局。既有评分也不代表本次修改后的画面质量。保持双门交叉控制、镜子规则、统一Player尺寸与移动参数，不运行整房Builder。

| 入口ID | Player Collider中心 | 支撑面 |
|---|---|---|
| `DEFAULT` | `(-3,-1.08)` | 底层地面 `Y=-2` |
| `FROM_FIRE_014` | `(-3,-1.08)` | 底层地面 `Y=-2` |
| `FROM_FIRE_015` | `(-3,-1.08)` | 底层地面 `Y=-2` |
| `FROM_FIRE_009` | `(0.45,1.92)` | 中央平台 `Y=1` |

返回点放在门内的安全区域，避免重新进入时落在关闭门外。入口ID仍各自唯一；三个底层入口共享安全Pose不合并ID。按实际`0.8×1.8`Player Collider检查，四个入口均不穿地形或关闭门，脚底距支撑面0.02 units；上层入口与最近出口的边缘净距1.05 units，还满足`RoomExit2D`严格大于1的释放条件，不停在等号边界。

| 出口 | 中心 | 目标Scene / 入口 |
|---|---|---|
| `Exit-A to FIRE_014` | `(-11.5,-0.8)` | `Fire_014 / FROM_FIRE_013` |
| `Exit-B to FIRE_015` | `(11.5,-0.8)` | `Fire_015 / FROM_FIRE_013` |
| `Exit-C to FIRE_009` | `(2.5,2.2)` | `Fire_009 / FROM_FIRE_013` |

- 三个出口保持原`1.2×2.4`Trigger；底部分别贴合路面`Y=-2`和平台`Y=1`，不再埋入Terrain。修正返回FIRE_009的来源入口，不依赖DEFAULT回退；三个目标Scene、来源入口和Build Settings条目已核对。
- 移除`X=-12、12 / Y=-2～0`的六格挡墙，疏通门外通往左右出口的走廊。左右门尺寸与位置不变，补`(-6,0)`、`(6,0)`两格墙帽，使门洞上沿与竖墙接合；开门区域无残留Terrain。
- 中央台阶去掉`(1,1)、(2,1)、(3,1)`，新增`(-2,-1)、(2,-1)、(3,-1)、(4,-1)`。两侧首阶支撑面`Y=0`、宽2 units，最高平台支撑面`Y=1`、宽3 units；从底层先升2 units再升1 unit，不再从右侧面对4 units高的台阶。Terrain共100格，保留纯色Tile、材质、碰撞及表面语义配置。
- 两块压力板底部贴合`Y=-2`路面，向对侧关闭门靠近。门与板的引用、状态、Collider尺寸及Prefab连接未改。**关闭门只能在保持关闭时充当止挡：Player踩下另一块板会释放该止挡，不能据此宣称镜像能始终停板。** 共享Jump也可能释放占用；完整双分支操作与容错保留待验证，不依赖修改镜像输入、门防夹或锁存规则补救。
- 镜头仍为固定单屏，相机`(0,0,-10)`、正交尺寸7，16:9可见范围`X±12.444 / Y±7`。三个完整出口框、入口、两门、两板及中央台阶位于该范围；Player与MirrorClone、占用板和对应门需同时可观察。装饰遮挡和实际运行构图本轮未复拍。

| RV项 | 本次结果与证据 |
|---|---|
| RV-01 / RV-02 | 通过入口静态子项：4个完整出生边界、连续支撑、出口间距及严格释放条件已核对；运行时重置见RV-11 |
| RV-03 / RV-04 / RV-05 | 台阶尺度、板底贴地、门洞净空及墙帽静态检查通过；实际跳跃、落地和防夹待验证 |
| RV-06 | 3个出口无Terrain重叠，目标资产、来源入口和Build Settings引用通过；完整解法与实际切场待验证 |
| RV-07 / RV-08 | 本房无敌人、岩浆、Spawner或射线路径；Tile碰撞/语义与既有门板控制引用未改，静态差异通过 |
| RV-09 | 待验证：合法静态放镜面保持；没有用分别移动或传送双角色替代共享输入解法验证，两门相互释放止挡仍是重点风险 |
| RV-10 | 16:9数学视野范围通过；实际Player生成、装饰遮挡与运行画面待验证 |
| RV-11 | 待验证：死亡、镜像清除、重置、切场沿用通用组件，入口已修复；本轮未运行这些路径 |
| RV-12 | YAML、本地引用、100格Tile索引/计数、Prefab覆盖和本文坐标通过静态检查；历史Builder禁止覆盖当前手工Scene，Editor回读待验证 |

本轮以最新Scene为基线只修改10个序列化块，其余141个块逐字节不变；保留新增雾、余烬、光环、材质、出口文字与颜色等视觉覆盖，以及外部新增序列化字段。写入前确认另一任务“fire013”已结束；Unity实例`W1@49ee0df8`当前仅加载`Snow_004`、dirty=false，未切场、保存或重载其他Scene。FIRE_013磁盘修改已完成，下一次正常打开时读取。

静态记录：`Temp/FireRoomRepair/Fire_013.verification.json`，本轮差异：`Temp/FireRoomRepair/Fire_013.applied.patch`。未运行Play Mode观察、自动测试、完整编译/重导入或人工试玩，不能宣称全房验收通过。历史Builder仅为旧灰盒入口，尚未同步本次坐标，禁止调用它覆盖正式Scene。

## 2026-08-31 增量视觉优化

- 本轮仅修改表现，Scene 是手工视觉配置的权威；`Fire013To015RoomBuilder`仍为多房灰盒重建入口，不得用它覆盖本次环境内容，本轮未运行 Builder。
- 新增独立 `Fire013 Environment Visuals`，复用既有火区洞穴、岩体、遗迹、底部残骸和柔雾 Sprite。01 色/雾底、02 极远轮廓、03 远岩体、04 中景遗迹、08 底部框景分别使用 `1 / 0.95 / 0.85 / 0.65 / 0.20` 水平视差，显式引用本房 Main Camera。固定镜头中不产生相机视差位移。
- 05 后部动态雾和 07 前部动态雾/粒子没有内容，省略；柔雾底图是静态表现，不声称实现动态雾。已有洞穴底图包含烘焙远景形状，不将其中形状宣称为独立视差层。
- 06 玩法层保持原 Grid 与 Prefab。`Terrain` 的 103 格仅换用 `Assets/Tiles/Fire/Fire013SolidTerrain.asset`：复用既有纯白 Sprite，颜色 `(0.10,0.065,0.057,1)`，无地面纹理；保持原 Tile 的 Grid Collider、Flags、单位变换及所有格子坐标。
- 表现树仅含 Transform、SpriteRenderer 和 ParallaxLayer2D；背景/中景排序 `-100～-34`，底部框景排序 `30/31`、最高点低于 `Y=-4.1`，不覆盖地板上方的玩法路线。未改 Light2D、Volume、Shader、共享材质或公共角色渲染规则。
- 保持原地形碰撞、表面语义、门板关系、入口出口、相机 `(0,0,-10)`/正交尺寸 `7`、镜子规则和重置引用。本次没有修改玩法代码或 Prefab。

### 评分与验证记录

- 固定 `1920×1080`、`16:9`、DEFAULT 无输入、运行约 `2.02s` 暂停取图，九项静态评分依次为 **36.5 → 47.0 → 55.5 → 57.5**；LIMBO 固定基准 `87.5`，动态 `N/A`。这是当前固定机位的暂定评分，未达到用户要求的 `70`。
- 前三轮接受；第四轮的底图替换因 Unity 被其他操作用于 `Snow_005` Play Mode 而无法同条件复拍，已按本轮变更清单撤回。停止属于编辑状态安全限制，不是连续两轮提升不足 1 分。
- 证据与九项评分：`Temp/W1VisualOptimize/Fire_013/20260831/review.md`；最佳截图 `round3.png`。Scene 磁盘版本与接受的第三轮快照一致（仅新增块空白清理）。
- RV-07：本次纯色视觉、格子与碰撞/语义静态一致性通过；RV-10：本次新增装饰在初始固定机位未遮挡两门两板。RV-12：旧序列化块除 Terrain 三处视觉字段及新增 Scene 根引用外保持不变；外部关闭 Decoration Renderer 的改动已保留，不计为本任务改动。
- **既有错误 RV-01/RV-10**：直接从 DEFAULT `(0,-1.08)` 进入时，中央台阶占用完整 Player 所需空间；Console 报 `Fire_013 entrance DEFAULT cannot contain the full Player collider.`，Player 未生成。固定镜头实际水平视野约 `[-12.444,12.444]`，左右出口中心 `X=±13.2` 在画外。本轮不改变出生点、地形或镜头，因此保留问题并持续扣分，不宣称全房验收通过。
- RV-02～06、RV-08～09、RV-11 未受修改且本轮未做完整运行重验。未运行 PlayMode 自动测试、完整 EditMode、完整编译、全量重导入或人工试玩；双角色辨识与路线、死亡重置、门控时序及场景切换仍待运行验证。已有 memoryless depth load/store 警告仍存在。

### 同日续跑与最终停止

- Unity空闲后恢复原条件复拍：`1920×1080`、固定相机、DEFAULT无输入、约`2.02s`，基线仍为 **57.5/100**。
- 第4轮实际完成复拍：柔雾替换硬切面底图后 **54.5（−3.0）**，因中央空间变空、洞穴纵深及辨识度下降而撤回。此前未完成的第4轮准备不计作额外低增益轮次。
- 第5轮从恢复后的57.5版本开始，只降低两侧岩体/遗迹Renderer的颜色与透明度，复评 **57.5（+0.0）**，改善不足以提高整数分，亦撤回。增益以该轮开始的保留版本计算，不把54.5→57.5误计为+3。
- **连续两轮提升不足1分，按用户条件停止；最佳保留57.5，未达到70。** LIMBO固定87.5，动态N/A；没有改变评分权重、玩法、碰撞、镜子、出生点或相机来补分。
- 最终Scene与续跑前逐字节一致；Unity已回到本房Edit Mode、dirty=false、无Prefab Stage。原有出生净空错误、侧出口画外与memoryless depth警告仍保留。
- 完整九项复评、候选图和回滚清单见 `Temp/W1VisualOptimize/Fire_013/20260831/resume1/review.md`；`resume1/baseline.png`同时代表最终保留画面。本次未运行自动测试或完整试玩。

### 更新优化 Skill 后的动态效果续跑（同日）

- 用户重新要求按更新后的 `w1-visual-optimize` 创建缺失动态效果并继续提升到 70，本次重新建立 57.5 基线。固定 1920×1080、16:9、DEFAULT 无输入、相机 `(0,0,-10)`/ortho 7、约 2 秒暂停复拍；五个完成轮次依次为 **59.0（+1.5）→61.5（+2.5）→63.0（+1.5）→65.0（+2.0）→66.0（+1.0）**。LIMBO 基准 87.5、动态 D=N/A。
- 新增 `05 Rear Dynamic Fog`（Parallax 0.78）：Rear Warm Fog Field 复用 LayeredFog2D Shader，排序 -18；新增可复用 `FireCavernEmberField2D.prefab`，固定随机种子 13013、最大 64 个稀疏余烬、排序 -16，Collision/Trigger/SubEmitter 关闭；Rear Cavern Focal Glow 复用 RadialCaveGlow2D，排序 -17。07 前景动态层仍不使用，避免遮挡玩法。
- 新增区域材质 `FireCavernRearFog`、`FireCavernEmberParticle`、`FireTerrainSilhouette`、`FireCavernFocalGlow`，以及 SoftEmberParticle2D/FireTerrainSilhouette2D Shader。Terrain 只替换 Renderer 材质，仍使用原 103 格和原碰撞/语义。三个出口仅有本房文字、文字尺寸/颜色和 Visual 色值覆盖，共享出口 Prefab、脚本及目标房间未改。门和压力板的状态颜色未动。
- 动态雾和余烬已用约 2/6/10 秒序列观察；余烬最终采样粒子数 39/40/38，持续缓慢漂移。只证明对应画面状态，不证明输入延迟、性能或全部重置路径。第五轮最佳图为 `dynamic_resume/round5_focal_glow_t2.png`，最佳 Scene 快照为 `dynamic_resume/scene_before_round6.unity`。
- 第六轮出口细光环候选新增 SoftPortalHalo2D Shader、FireExitHalo 材质及三个出口 Visual 材质覆盖。初次光环过亮，已调暗减细；调色后截图被外部退出 Play Mode 打断，随后 Fire_013 出现外部未保存编辑（dirty=true），因此未重载、未保存、未覆盖。**第六轮尚未验收；当前 Scene 含候选及外部变化，66.0 仅代表第五轮已验证快照。** 不可直接用快照覆盖当前 Scene。
- 本次未达到 70，且尚未触发连续两轮提升不足 1 分；暂停原因是未保存编辑保护，低提升连续计数为 0。恢复时先保留/解决当前编辑，再核对差异并补第六轮同条件复拍，不把编辑态截图计分。
- 已验证五轮相对本次基线仅新增 11 个序列化块、修改 5 个既有表现/出口视觉块，其余 134 个既有块逐字节一致；Collider、Tilemap 格子、表面语义、镜子、门板关系、出生点、出口目标及相机配置未改。当前外部重序列化新增 `roomCamera: {fileID: 0}` 等内容未由本任务覆盖。
- 完整九项表、逐轮证据、资源参数和静态核对见 `Temp/W1VisualOptimize/Fire_013/20260831/dynamic_resume/review.md` 与 `static_contract_check.json`。原有 DEFAULT 净空失败、Player 未生成及左右出口画外仍保留并持续扣分；未做自动测试、完整编译/重导入或完整人工试玩，不宣称全房验收通过。
- 收尾发现余烬速度 X/Y 为双常量、Z 为单常量而产生 Unity 曲线模式警告；已仅将新 Prefab 的 Z 模式改为双常量（两端仍为 0），回读三轴一致。未保存/reload dirty Scene；修正后时间序列和警告消失待验证。此前 66.0 与 2/6/10 秒序列属于修正前画面，Scene 快照并非完整资源冻结副本。
