# FIRE_015：喷发接力

> 2026-08-31 显示调整：按用户要求关闭本房 `Decoration` 的 `Tilemap Renderer`，保留对象、Tile 与坐标；本文中的该层提示位置仅作设计参考，当前不显示、不再要求提示标记可见。碰撞、镜子放置规则与解法不变。

- 状态：灰盒中；Scene：`Assets/Scenes/Levels/Fire/Fire_015.unity`。
- 连接：`FIRE_013—FIRE_015—FIRE_016`，右侧近入口出口返回`FIRE_013`，左侧门后出口前往`FIRE_016`。
- 入口与出口：保留唯一`DEFAULT`安全入口；FIRE_013至FIRE_017之间的已实现双向连接使用`FROM_<来源房间ID>`入口。
- 固定单屏，Grid `1×1`，边界`X[-15,15] Y[-7,7]`，正交尺寸`7`。

## 2026-08-31 房间几何修复

本节替代下文涉及同一对象的旧坐标、旧入口目标和旧检查结论；未提及的玩法规则及美术记录不变。对应RV结果、设计冲突和验证范围见[FIRE_REPAIR_REVIEW.md](FIRE_REPAIR_REVIEW.md)。当前仍为待运行验证的灰盒，不代表全房验收通过。

DEFAULT放镜点为(-0.5,-1.08)，两个来源入口为(3.5,-1.08)，保证重置后位于安全的谜题侧。删除与固定岩浆重叠的Terrain(-4,-3)、(-3,-3)。Door-A移到(-8.5,-1)，墙帽同步为X=-9,Y=0～5；Plate-A为(8,-1.85)。投火者移到(11.5,-0.5)，支撑格(11,-2)，止挡格(9,-2)、低顶(8,0)，镜像停点X=8.6。正交尺寸7.5；双喷发均继承修正后的1×4危险边界。

| 入口ID | Player Collider中心 |
|---|---|
| `FROM_FIRE_013` | `(3.5,-1.08)` |
| `FROM_FIRE_016` | `(3.5,-1.08)` |
| `DEFAULT` | `(-0.5,-1.08)` |

| 已实现出口 | 中心 | 目标Scene/入口ID |
|---|---|---|
| `Exit-B to FIRE_013` | `(5.5,-1)` | `Fire_013/FROM_FIRE_015` |
| `Exit-A to FIRE_016` | `(-12.5,-1)` | `Fire_016/FROM_FIRE_015` |

本轮增量编辑Scene，未运行整房Builder。当前Scene是落位权威；历史重建入口尚未同步本轮布局，禁止用其覆盖正式Scene。仅FIRE_011构建器的共用喷发Prefab尺寸计算同步修复，不表示其旧房间布局已同步。未运行Unity自动测试或人工试玩，碰撞重建、完整解法、重置和画面遮挡仍需验证。

## 布局与解法

```text
E ◀ D ─ 喷发B ─ 岩浆坑 ─ 喷发A ─ P/M ─ [A]█ ← H
```

玩家观察两处固定周期喷发，在冷却窗口放镜并左行；MirrorClone右行踩板、吸引投火者并维持左门。玩家跨越固定岩浆坑与两处喷发后穿门前往`FIRE_016`。核心是让周期窗口、开门窗口和替身窗口重合。

## Prefab、Tilemap与重置

- `Terrain`为安全静态地形；固定岩浆使用`Hazard` Tilemap、Trigger、显式`Hazard`语义和`Hazard2D`。
- `Plate-A (8.5,-1.7)`控制`Door-A (-9.5,-1)`；喷发位于`(-1.5,0)`与`(-6.5,0)`；固定投火者位于`(12.5,-1.5)`向左。
- 所有动态对象使用通用Prefab，无Spawner或房间专用代码。
- 重置恢复喷发初始相位、门、压力板、敌人和火球；MirrorClone单独死亡不整体重置。
- 风险：未人工试玩，连续窗口和跳跃容错待验证。
