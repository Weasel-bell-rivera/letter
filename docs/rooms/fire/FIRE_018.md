# FIRE_018：双线引火

> 2026-08-31 显示调整：按用户要求关闭本房 `Decoration` 的 `Tilemap Renderer`，保留对象、Tile 与坐标；本文中的该层提示位置仅作设计参考，当前不显示、不再要求提示标记可见。碰撞、镜子放置规则与解法不变。

## 2026-08-31 房间几何修复

本节替代下文涉及同一对象的旧坐标、旧入口目标和旧检查结论；未提及的玩法规则及美术记录不变。对应RV结果、设计冲突和验证范围见[FIRE_REPAIR_REVIEW.md](FIRE_REPAIR_REVIEW.md)。当前仍为待运行验证的灰盒，不代表全房验收通过。

DEFAULT入口改为(-9.4,2.92)。将上层投火者及锁存板移到关闭门的左侧，分别为(4.5,2.5)、(2.5,2.625)，保持“敌人→锁存板→诱敌角色”的水平弹道顺序；Door-Upper仍为(6.5,3)。敌人右侧到门左侧保留1 unit地面落脚空间，供开门后越过敌人并落地通过门洞。下层投火者与锁存板、AND关系和不新增FIRE_019出口的边界保持。

| 入口ID | Player Collider中心 |
|---|---|
| `DEFAULT` | `(-9.4,2.92)` |
| `FROM_FIRE_017` | `(-9.4,2.92)` |

| 已实现出口 | 中心 | 目标Scene/入口ID |
|---|---|---|
| `Exit-Back-to-FIRE_017` | `(-11.5,3)` | `Fire_017/FROM_FIRE_018` |

本轮增量编辑Scene，未运行整房Builder。当前Scene是落位权威；历史重建入口尚未同步本轮布局，禁止用其覆盖正式Scene。仅FIRE_011构建器的共用喷发Prefab尺寸计算同步修复，不表示其旧房间布局已同步。未运行Unity自动测试或人工试玩，碰撞重建、完整解法、重置和画面遮挡仍需验证。

## 状态与权威来源

- 房间ID：`FIRE_018`。
- 区域：火之区域。
- 当前状态：灰盒中；用户已确认与`FIRE_017`相连并要求制作；通往`FIRE_019`的地图连接已登记，但尚未增量实现到本房Scene。
- Unity Scene：`Assets/Scenes/Levels/Fire/Fire_018.unity`。
- 区域规则：`docs/regions/FIRE_REGION.md`。
- 地图连接：`docs/maps/MAP.md`；左侧连接`FIRE_017`，右侧规划连接`FIRE_019`。
- 静态几何：`docs/systems/LEVEL_GEOMETRY_SYSTEM.md`。
- 镜子与镜像：`docs/MIRROR_MECHANIC.md`。
- 门与开关：`docs/systems/DOOR_SYSTEM.md`。
- 重置：`docs/systems/RESET_SYSTEM.md`。

## 地图连接

- `FIRE_017`右侧出口进入本房左上角`DEFAULT`入口。
- 本房左上角`Exit-Back-to-FIRE_017`返回`Fire_017/FROM_FIRE_018`。
- 本房现有灰盒仍以打开`Door-Upper`作为机关完成目标，Scene中尚未创建通往其他房间的出口。
- 地图已登记本房右侧连接`FIRE_019`，且`FIRE_019`无出口灰盒已经创建；用户要求本阶段暂不考虑出口，因此本房不增加`Exit-to-FIRE_019`，既有Scene保持不变。

## 房间定位

- 房间类型：火球诱导与镜像分路组合房。
- 主要目标：分别诱导上、下两条通道中的水平火球命中各自的锁存开关；两块开关均激活后，上方门才打开。
- 预期洞察：Player与MirrorClone可以在上下两条独立通道中同时成为诱敌目标；火球锁存允许玩家分两步完成AND条件。
- 失败压力：中；火球可以杀死Player或MirrorClone，但谜题可通过重新放镜或手动重置无惩罚重试。
- 预计完成时间：待灰盒验证。

## 已批准机制与排除项

### 使用机制

- 地面镜与MirrorClone：输入变换后独立物理，规则见`docs/MIRROR_MECHANIC.md`。
- 两个固定Scene投火者：使用`HorizontalFireballEnemy2D`，分别服务上、下通道，不使用Spawner。
- 两块`FireballLatch`压力板：每块只响应水平火球，命中后在当前房间尝试内保持激活。
- 一扇普通门：唯一控制源为两块`FireballLatch`，逻辑模式为AND，初始关闭。

### 明确不包含

- 不新增火球、镜像、门或压力板的房间专用行为。
- 不使用永久锁存双压力板门控组；本房两块火球锁存板在手动重置、Player死亡或重新进房时恢复未激活。
- 不使用运行时敌人生成、Spawner、房间专用脚本或候选火区机制。
- 当前不批准岩浆、周期喷发、升降岩浆、移动平台或额外门。

## 空间要求

- 房间由上下两条横向通道组成，入口安全区位于左上角。
- 两条通道在视觉上清楚分层，并通过中部的镜像放置/分路位置建立解题关系。
- Player必须能够在到达中部前观察到上、下两条火线、两块锁存板和上方关闭门的因果布局。
- 中部必须提供静态、水平、安全且空间充足的合法地面镜放置面。
- 放镜后，非对称地形应使MirrorClone能够进入下方通道，而Player继续留在上方通道；不得改变共享输入或镜像物理规则来强行分路。
- 上、下通道各自的投火者、诱敌位置和`FireballLatch`必须水平共线。正确弹道应先命中对应锁存板，不能先撞门、Terrain、另一角色或另一个锁存板。
- 上方门是完成后的唯一前进阻挡；门洞外不得留下标准跳跃可绕过的缝隙。
- 房间边界采用`X[-12,12] Y[-5,6]`的固定单屏灰盒；静态地形使用标准Tilemap层。

## 标准网格布局

- Grid：`1×1 Unity unit`。
- 上方通道地面：`Y=1`，中部`X=-1～0`留作下落缺口。
- 下方通道地面：`Y=-5`。
- 左上入口：`(-10.5,2.92)`；建议放镜点位于上层中部偏左。
- `Door-Upper`位于`(6.5,3)`，门上方以Terrain封闭至天花板。

## 对象与控制关系

| 实例ID | 对象 | 初始状态 | 作用 |
|---|---|---|---|
| `Enemy-Upper` | `HorizontalFireballEnemy2D`固定Scene实例，`(10.5,2.5)` | `Watching`、面向左 | 被上方Player诱导，向`Latch-Upper`发射水平火球 |
| `Enemy-Lower` | `HorizontalFireballEnemy2D`固定Scene实例，`(10.5,-3.5)` | `Watching`、面向左 | 被下方MirrorClone诱导，向`Latch-Lower`发射水平火球 |
| `Latch-Upper` | `PressurePlate2D / FireballLatch`，`(8.5,2.625)` | 未激活 | 上方火球命中后锁存本次尝试的第一项门条件 |
| `Latch-Lower` | `PressurePlate2D / FireballLatch`，`(8.5,-3.375)` | 未激活 | 下方火球命中后锁存本次尝试的第二项门条件 |
| `Door-Upper` | `Door2D`，`(6.5,3)` | 关闭、`ControlLogic.And` | 仅当两块锁存板均激活时开启 |
| `MirrorHint-Mid` | 无碰撞视觉提示 | 可见 | 标示中部建议放镜点，不参与放置合法性判断 |

控制关系：

```text
Enemy-Upper火球 → Latch-Upper ┐
                               ├─ AND → Door-Upper
Enemy-Lower火球 → Latch-Lower ┘
```

- 两块锁存板不要求在同一物理帧命中；任意顺序均可。
- 只激活其中一块时，`Door-Upper`保持关闭。
- 第二块激活后，`Door-Upper`开启并保持到本次房间尝试结束。
- Player、MirrorClone和普通动态物体不能直接激活两块锁存板。

## 预期流程

1. Player从左上角出生，沿上方通道前进并观察上下两条火线和关闭的`Door-Upper`。
2. Player到达中部合法放镜点并放置地面镜，使Player留在上方、MirrorClone进入下方通道。
3. MirrorClone在下方诱敌位置勾引`Enemy-Lower`发射；火球命中`Latch-Lower`并锁存，下方条件完成，但门仍关闭。
4. Player在上方诱敌位置勾引`Enemy-Upper`发射；火球命中`Latch-Upper`并锁存。
5. 两块锁存板均激活后，`Door-Upper`开启，Player从上方通路继续前进。

上下两个锁存条件允许交换完成顺序，但标准空间引导应优先让玩家理解“先用MirrorClone完成下方条件，再由Player完成上方条件”。

## 失败、重置与软锁

- Player被火球或投火者命中：执行完整房间重置；镜像清除、镜子回手、两块锁存板恢复未激活、门恢复关闭、两名投火者和全部在途火球恢复初始状态。
- MirrorClone被火球命中：只执行镜像死亡与镜子自动回收；已经命中的`FireballLatch`保持激活，Player可以回到中部重新放镜继续尝试。
- 手动重置：两块锁存板和门恢复初始状态，两名投火者恢复`Watching`并清除在途火球。
- 重新进入房间或场景切换：不保留两块火球锁存、门状态、镜像或在途火球。
- 任意一次诱敌失败后，Player必须能安全返回中部放镜点或按`R`重置；上下通道不得形成无法返回的单向软锁。
- 门的关闭路径遵循通用防夹规则，但角色碰门不能成为开门信号。

## 相机与信息可见性

- 镜头模式：固定单屏；中心`(0,0.5)`，正交尺寸`7`。
- 必须同时可见：左上入口、中部放镜点、上下两条通道的关键诱敌位置、两块锁存板和`Door-Upper`。
- 不绘制瞄准线或机关连线；通过水平共线布局、火球运动、锁存反馈和门开启表现表达因果。
- 如果单屏无法保持Player约占屏幕高度`12%–14%`并清楚展示两条通道，则改用已批准的边界跟随模式，不得缩小Player。

## 最小验收标准

- 房间恰好包含两条横向通道、两个固定投火者、两块`FireballLatch`和一扇AND控制的上方门。
- 入口出生点位于左上角，中部存在明确且合法的地面镜放置点。
- MirrorClone可以按通用规则进入下方通道诱导下方火球，Player可以留在上方诱导上方火球。
- 任意单块锁存板激活时门保持关闭；两块均激活后门才开启。
- 两块锁存板可按任意顺序完成，MirrorClone之后死亡不清除已经完成的锁存条件。
- 手动重置、Player死亡和重新进入房间会清除两块锁存并关闭门。
- 不存在绕门、误击另一块板、火球先撞Terrain或失败后无法重试的路径。

## 实施记录与未验证风险

- 已完成：FIRE_017双向连接、标准Tilemap静态灰盒、固定镜头、两名固定投火者、两块`FireballLatch`、AND门、入口与返回出口。
- Editor构建器：`Assets/Editor/Fire018RoomBuilder.cs`；动态对象保持共享Prefab连接，无房间专用运行时代码。
- 自动验证：Builder内验证对象数量、Prefab组件、锁存模式、AND引用、出口目标、Player不序列化及Terrain Collider；Unity Console无编译或构建错误。
- 人工试玩：未执行；诱敌距离、弹道净空、共享输入下的上下分路和操作容错尚未验证。

## 2026-08-31 火区剪影素材接入

本轮按“使用画的图优化fire018”增量修改手工Scene，仅调整环境表现。上文几何修复后的坐标、上下双火线、双锁存板AND关系、入口出口、相机和重置配置保持不变；不运行旧`Fire018RoomBuilder`，它仍不能覆盖当前Scene。

- 素材：复用`Assets/Art/Fire/Environment/Silhouettes20260831/`中的01～08八张图片，未改写PNG或已有导入切片。03使用两个已有大切片，04和05只使用完整主体切片，不使用自动切出的微小边缘碎片。
- 独立表现根：`Fire018 Environment Silhouettes`，16个GameObject、10个SpriteRenderer、5个`ParallaxLayer2D`。根及后代无Collider、Rigidbody、玩法组件或重置参与。
- 岩体使用新建的局部材质`Assets/Materials/Regions/Fire/Fire018EnvironmentSilhouette.mat`，复用已有`SolidSilhouetteSprite.shader`，只采样图片Alpha形成自然轮廓，屏蔽内部颗粒与裂纹。未修改共享Shader、角色、敌人、门、压力板或出口Prefab。
- 雾光底保留宽阔暗琥珀中心；叠加纯色透明底层抬高暗部、压低熔光对比。它属于最远颜色底，不是岩体或可接触岩浆。
- `Terrain`的90个格子改用已有`Assets/Tiles/Fire/Fire013SolidTerrain.asset`，颜色`(0.1, 0.065, 0.057)`、纯白Sprite、Grid Collider类型。格子坐标、Tile矩阵、Flags、Collider与表面语义不变；不修改该共享Tile本体。区域Palette仍为`Assets/TilePalettes/Fire.prefab`，只在空位`(9,0)`补入此Tile，原9格保留。
- `Decoration` Renderer继续关闭，不恢复放镜提示。没有新增岩浆、火星或装饰性危险信号。

| 功能层 | 内容 | Order | 水平跟随倍率 |
|---|---|---|---:|
| 01 颜色/雾光底 | 01底图、低对比暖色暗部底 | -100、-95 | 1.00 |
| 02 极远轮廓 | 02大洞壁 | -85 | 0.95 |
| 03 远景环境 | 03洞壁肩部、遥远岩柱 | -70、-69 | 0.85 |
| 04 中景环境 | 04粗岩柱、05悬垂岩、06烧焦管段 | -45～-43 | 0.65 |
| 05 后部动态雾 | 本轮省略；保留静态雾光，不宣称实现动态烟雾 | — | — |
| 06 玩法层 | 原Grid、角色与通用玩法Prefab | 保持原值 | 无视差 |
| 07 前部动态粒子 | 本轮省略，避免干扰火球与锁存反馈 | — | — |
| 08 最前景 | 07左下岩壁、08右上岩壁 | 30、31 | 0.20 |

五个视差组件显式引用本房Main Camera Transform，均只跟随X。固定镜头`(0,0.5,-10)`、正交尺寸7、16:9不变，本房不通过移动相机制造视差。视野`X[-12.444,12.444] Y[-6.5,7.5]`；雾光底覆盖约28.76×16.19 units。左前景包络最右约`X=-11.61`、最高`Y=-0.22`，右前景最左约`X=11.58`、最低`Y=1.05`，避开双通道与出生/返回出口的角色视觉空间。

### 本轮证据与检查范围

证据目录：`Temp/W1VisualOptimize/Fire_018/20260831/`。`baseline.unity`保留任务开始时已含用户修改的版本；`change-manifest.json`、`refinement-manifest.json`、`edge-clearance.json`记录本轮增量；`contract-check.json`记录序列化对照；`review.md`包含九项评分和截图。

先使用隔离Preview Scene检查构图，没有中断当时正在运行的Fire_013。它自行退出且无dirty Scene后才打开Fire_018，完成约1秒的出生状态运行取图。`runtime-before.png`是在暂停的同一状态中临时恢复旧Tile并隐藏新环境的基线外观，取图后立即还原，未保存临时状态；最终图为`runtime-after-1.png`，1920×1080、相同相机、出生点与约1秒状态。两次短暂观察后均退出Play Mode，最终Fire_018为非运行、非dirty状态。

静态暂评分S：38.5→58.0；固定`limbo-official-stills-v1`为87.5。本次不是完整游戏质量评价，动态D为N/A。残余差距主要是现有彩色角色/机关与环境的风格差异、固定双通道构图及出口占位视觉；未为此改动公共资产或玩法几何。当前取图后的Console查询无Error/Warning；这不是完整编译或自动测试通过。

| RV项 | 本轮结果与范围 | 证据或待验证内容 |
|---|---|---|
| RV-01 | 出生状态通过；其他恢复状态待验证 | DEFAULT运行出生`(-9.4,2.92)`，Player边界`X[-9.8,-9.0] Y[2.02,3.82]`；FROM_FIRE_017保持相同配置；未执行死亡重置 |
| RV-02 | 静态净距通过；防回触流程未重验 | 出口Trigger最右`X=-10.9`，出生Collider最左`X=-9.8`，水平净距1.1 unit |
| RV-03 | 本轮不改变净空；完整路线待验证 | 90个Tile坐标、矩阵和全部原Transform一致；未以此声称跳跃或镜像分路通过 |
| RV-04 | 受影响的视觉遮挡通过；原对象落位保留 | 两名敌人、两块板和门可见且贴合原通道面；原Collider与Prefab覆盖未改变 |
| RV-05 | 本轮未改；开门/防夹/绕门待验证 | 门位置`(6.5,3)`及Collider`X[6,7] Y[2,4]`保留 |
| RV-06 | 本轮未改连接；切场景待验证 | 仅保留返回FIRE_017；不新增规划中的FIRE_019出口 |
| RV-07 | 本轮视觉与配置对照通过 | 新装饰无Collider/语义；Terrain由旧纹理换为同尺寸纯色格，原碰撞与语义序列化块一致；本房不新增岩浆 |
| RV-08 | 引用对照通过；完整作用路径待验证 | 所有PrefabInstance与控制引用逐块一致；未执行两块锁存及开门完整流程 |
| RV-09 | 配置未改；运行验证未执行 | 未放镜、未产生MirrorClone，不声称双角色路线或放置反馈通过；全部背景排序低于MirrorClone的-10 |
| RV-10 | 固定出生画面通过；其他机制状态待验证 | 16:9运行图中Player、上下敌人/锁存板和门可见；相机配置逐块不变；未验证镜像、火球运动和失败状态 |
| RV-11 | 本轮未改；待验证 | 重置与生命周期配置保持原值；未执行完整死亡、手动重置或重新进房流程 |
| RV-12 | 本轮增量资产与引用通过 | 原82个序列化块保留，80块逐字不变；另2块只涉及Terrain视觉缓存和SceneRoots追加。新增47块仅为表现对象/视差。Palette只补1格；旧Builder仍标记为历史入口 |

未运行PlayMode自动测试、完整EditMode、完整编译、全量重导入或完整人工试玩；本节不替代前述灰盒与几何修复的待验证项。
