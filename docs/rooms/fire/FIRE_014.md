# FIRE_014：交叉火线

> 2026-08-31 显示调整：按用户要求关闭本房 `Decoration` 的 `Tilemap Renderer`，保留对象、Tile 与坐标；本文中的该层提示位置仅作设计参考，当前不显示、不再要求提示标记可见。碰撞、镜子放置规则与解法不变。

- 状态：灰盒中；Scene：`Assets/Scenes/Levels/Fire/Fire_014.unity`。
- 连接：仅`FIRE_013`，采用完成谜题后返回的死端挑战房。
- 入口与出口：保留唯一`DEFAULT`安全入口；FIRE_013至FIRE_017之间的已实现双向连接使用`FROM_<来源房间ID>`入口。
- 固定单屏，Grid `1×1`，边界`X[-15,15] Y[-7,7]`，正交尺寸`7`。

## 2026-08-31 房间几何修复

本节替代下文涉及同一对象的旧坐标、旧入口目标和旧检查结论；未提及的玩法规则及美术记录不变。对应RV结果、设计冲突和验证范围见[FIRE_REPAIR_REVIEW.md](FIRE_REPAIR_REVIEW.md)。当前仍为待运行验证的灰盒，不代表全房验收通过。

DEFAULT及FROM_FIRE_013改到(-0.5,-1.08)，不再从左门外出生直接绕过主谜题。右投火者为(9.5,-0.5)，支撑格(9,-2)；止挡格(7,-2)，低顶(6,0)，镜像停点X=6.6；门墙帽补齐(-8,0)。镜头正交尺寸改为全局推荐范围内的7.5，在16:9下可见宽约26.67，容纳左投火者及出口。保留左投火者位置与朝向；“穿门后该门挡住来自左侧的火球”仍是未解决的设计矛盾。

| 入口ID | Player Collider中心 |
|---|---|
| `FROM_FIRE_013` | `(-0.5,-1.08)` |
| `DEFAULT` | `(-0.5,-1.08)` |

| 已实现出口 | 中心 | 目标Scene/入口ID |
|---|---|---|
| `Exit-A to FIRE_013` | `(-10.3,-1)` | `Fire_013/FROM_FIRE_014` |

本轮增量编辑Scene，未运行整房Builder。当前Scene是落位权威；历史重建入口尚未同步本轮布局，禁止用其覆盖正式Scene。仅FIRE_011构建器的共用喷发Prefab尺寸计算同步修复，不表示其旧房间布局已同步。未运行Unity自动测试或人工试玩，碰撞重建、完整解法、重置和画面遮挡仍需验证。

## 布局与解法

```text
H-2 → E ─ D ─── P/M ─── [A]█ ← H-1
```

MirrorClone右行接近`Plate-A`并吸引右侧投火者，Player左行穿门；门左侧投火者迫使玩家利用门关闭后的实体遮挡，再进入返回`FIRE_013`的出口。核心洞察是连续判断两个投火者的攻击带与门的遮挡。

## Prefab与重置

- `Plate-A (6.5,-1.7)`控制`Door-A (-7.5,-1)`。
- 固定Scene投火者位于`(11.5,-1.5)`向左和`(-12.5,-1.5)`向右；不使用Spawner。
- Player中弹完整重置；MirrorClone中弹只回收镜子并释放压力板；门和在途火球遵循通用重置。
- 风险：未人工试玩，双火线锁定时机待验证。
