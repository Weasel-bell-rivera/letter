# FIRE_017：升降熔潮

> 2026-08-31 显示调整：按用户要求关闭本房 `Decoration` 的 `Tilemap Renderer`，保留对象、Tile 与坐标；本文中的该层提示位置仅作设计参考，当前不显示、不再要求提示标记可见。碰撞、镜子放置规则与解法不变。

## 2026-08-31 房间几何修复

本节替代下文涉及同一对象的旧坐标、旧入口目标和旧检查结论；未提及的玩法规则及美术记录不变。对应RV结果、设计冲突和验证范围见[FIRE_REPAIR_REVIEW.md](FIRE_REPAIR_REVIEW.md)。当前仍为待运行验证的灰盒，不代表全房验收通过。

两个来源入口均移到DEFAULT安全侧(2,-1.08)。Door-A移到(-8.5,-1)，墙帽同步到X=-9,Y=0～5；Plate-A移到(8,-1.85)。止挡格(9,-2)、低顶(8,0)，投火者为(11.5,-0.5)，支撑格(11,-2)，镜像停点X=8.6。两池升降岩浆的低位中心下调为(-1.5,-2.6)、(-6.5,-2.6)，低位Trigger顶边Y=-2.1，低于安全路面0.1，升高距离仍为4 units。左右出口为(-12.5,-1)、(11.5,-1)，正交尺寸7.5。建议先在X=2观察，冷却时进入X≈0的地面放镜点再左行；不要把喷发危险阶段中的位置记为永久安全出生点。共同低位/冷却及穿门窗口仍待验证。

| 入口ID | Player Collider中心 |
|---|---|
| `FROM_FIRE_018` | `(2,-1.08)` |
| `DEFAULT` | `(2,-1.08)` |
| `FROM_FIRE_016` | `(2,-1.08)` |

| 已实现出口 | 中心 | 目标Scene/入口ID |
|---|---|---|
| `Exit-A to FIRE_016` | `(-12.5,-1)` | `Fire_016/FROM_FIRE_017` |
| `Exit-B to FIRE_018` | `(11.5,-1)` | `Fire_018/FROM_FIRE_017` |

本轮增量编辑Scene，未运行整房Builder。当前Scene是落位权威；历史重建入口尚未同步本轮布局，禁止用其覆盖正式Scene。仅FIRE_011构建器的共用喷发Prefab尺寸计算同步修复，不表示其旧房间布局已同步。未运行Unity自动测试或人工试玩，碰撞重建、完整解法、重置和画面遮挡仍需验证。

- 状态：灰盒中；Scene：`Assets/Scenes/Levels/Fire/Fire_017.unity`。
- 地图连接：左侧连接`FIRE_016`，右侧连接`FIRE_018`。
- 入口与出口：保留`DEFAULT`安全入口；FIRE_013至FIRE_018之间的已实现双向连接使用`FROM_<来源房间ID>`入口。右侧`Exit-B to FIRE_018`进入`Fire_018/DEFAULT`，从FIRE_018返回时使用`FROM_FIRE_018`入口。
- 固定单屏，Grid `1×1`，边界`X[-15,15] Y[-7,7]`，正交尺寸`7`。

```text
E ◀ D ─ 升降岩浆B ─ 喷发B ─ 升降岩浆A ─ 喷发A ─ P/M ─ [A]█ ← H
```

玩家观察两处喷发和两池熔潮的固定周期，在共同低位/冷却窗口放镜左行；MirrorClone右行踩板并吸引投火者。Player连续穿过两段升降岩浆和喷发区后穿门。

- 两个`RisingLava2D`实例位于`(-1.5,-2.5)`与`(-6.5,-2.5)`，使用批准的默认周期和`4 units`升高距离；不运行时修改Tilemap。
- `Plate-A (5.5,-1.7)`控制`Door-A (-10.5,-1)`；喷发位于`(0,0)`和`(-4.5,0)`；投火者`(12.5,-1.5)`向左。
- 左侧出口连接`Fire_016/FROM_FIRE_017`；右侧边墙增量打开两格通道并连接`Fire_018/DEFAULT`。所有动态对象使用现有通用Prefab，无Spawner或房间脚本。
- 重置恢复喷发、门、压力板、敌人和火球初始状态。未人工试玩，综合窗口和跳跃容错待验证。
