# WIND_014：弯折动力

## 状态

- 当前状态：灰盒已实现
- 是否允许制作灰盒：是（用户已明确要求实现后续可制作的风区关卡）
- Unity Scene：`Assets/Scenes/Levels/Wind/Wind_014.unity`

## 地图登记

- 地图编号：`WIND_014`
- 地图来源：`docs/maps/MAP.md`
- 出口目标：`Wind_015`，使用目标房间`DEFAULT`入口
- 本房间只使用地图中已有连接，不修改世界结构

## 房间定位

- 主要机制：导风板、涡轮与门
- 核心洞察：把水平风转向涡轮，以输出风持续开启门。
- 难度定位：随编号递增的组合教学灰盒
- 预计完成时间：`1～3分钟`

## ASCII灰盒

```text
入口 P ── 安全观察区 ── [ 导风板、涡轮与门 ] ── 安全回收区 ── E 出口
████████████████████████████████████████████████████████████████
```

具体地形保持单屏可读；导风板房间额外包含中央竖墙与上层落点，门房间的门、控制源和结果必须同时可见。

## 规则与重置

- 静态地形使用七层标准Tilemap结构，`Terrain`提供安全`StaticSolid`语义和连续复合碰撞。
- 所有动态玩法对象均使用通用Prefab连接实例，不在Scene中复制核心机制。
- Player和MirrorClone遵循统一风力、碰撞、危险与镜像规则。
- 手动重置、Player死亡和重新进入房间恢复所有周期、门、导风板、涡轮、敌人与生产器初态。
- MirrorClone单独死亡只回收镜像；除通用压力板占用释放外，不整体重置房间。
- 入口和出口两端保留安全站立区，不设置出生即受击，不引入房间专用运行时代码。

## 镜头

- 模式：固定单屏；相机中心`(0,0)`；正交尺寸`7`。
- 必须同时显示入口、主要机制、失败路径和出口。
- 不修改Player尺寸，无房间专用镜头例外。

## 验收标准

- Scene、文档和地图编号一致。
- 七个标准Tilemap层、一个通用Player生成器、一个`DEFAULT`入口和一个连接出口存在。
- 导风板、涡轮与门全部保持对应通用Prefab连接，实例只覆盖批准的方向、范围、周期或控制引用。
- 没有房间本地Player、房间专用玩法脚本或按对象名称推断玩法语义。
- 失败后可按统一房间重置规则无惩罚重试。

